<!DOCTYPE html>
  <html>
  <head> 
    <?php require_once("../ajout/ongbase.php");
          /*$site="SELECT DISTINCT nomsite FROM site ORDER BY nomsite";
          $sit=$pdo->prepare($site);
          $sit->execute();

          $comm="SELECT DISTINCT codecommune FROM commune ORDER BY codecommune";
          $com=$pdo->prepare($comm);
          $com->execute();
          $dis="SELECT DISTINCT codedistrict FROM district ORDER BY codedistrict";
          $di=$pdo->prepare($dis);
          $di->execute();*/  
        ?>
        <meta charset="utf-8" />
        <link rel="stylesheet" type="text/css" href="css/style.css" />
        <link rel="stylesheet" type="text/css" href="css/a.min.css"/> 
  </head>
  <body>
  <h1>MBOLA TSY VITA</h1>  
  </body>
  </html>
